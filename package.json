{
  "name": "cryptofeecalc-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate-robots.js && nuxt dev",
    "build": "node scripts/generate-robots.js && nuxt build",
    "start": "nuxt preview",
    "type-check": "nuxt typecheck"
  },
  "dependencies": {
    "nuxt": "^3.12.4"
  },
  "devDependencies": {
    "@types/node": "^25.0.6",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.2.2"
  }
}
